apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sprint-boot-app.fullname" . }}-test-connection"
  labels:
    {{- include "sprint-boot-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sprint-boot-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
