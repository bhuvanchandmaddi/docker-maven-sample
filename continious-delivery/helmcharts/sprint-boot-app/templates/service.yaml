apiVersion: v1
kind: Service
metadata:
  name: {{ include "sprint-boot-app.fullname" . }}
  labels:
    {{- include "sprint-boot-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sprint-boot-app.selectorLabels" . | nindent 4 }}
